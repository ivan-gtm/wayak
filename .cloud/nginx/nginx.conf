server {
    listen 8001;
    listen [::]:8001;
    root /application/public;
    index index.php index.html index.htm index.nginx-debian.html;

    server_name wayak.com www.wayak.com;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    location ~ \.php$ {
      fastcgi_index index.php;
      fastcgi_pass wayak-server:9000; # address of a fastCGI server
      fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
      fastcgi_param PATH_INFO $fastcgi_path_info;
      fastcgi_read_timeout 3600;
      include fastcgi_params;
    }
}

# server {
#   listen 8001;
#   client_max_body_size 100M;
#   index index.php index.html index.htm;
#   root /application/public; # default Laravel's entry point for all requests

#   access_log /var/log/nginx/access.log;
#   error_log /var/log/nginx/error.log;

#   location / {
#     # try to serve file directly, fallback to index.php
#     try_files $uri /index.php?$args;
#   }

#   location ~ \.php$ {
#     fastcgi_index index.php;
#     fastcgi_pass scrapper-server:9000; # address of a fastCGI server
#     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     fastcgi_param PATH_INFO $fastcgi_path_info;
#     fastcgi_read_timeout 3600;
#     include fastcgi_params;
#   }
# }
